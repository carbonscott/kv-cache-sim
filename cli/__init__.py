"""Stage 2 front-ends over the pure Stage 1 engine.

The engine (sim/) stays pure and prints nothing. This package holds the parts that
talk to a person: a shared command session, ledger formatting, and the REPL loop.
"""
